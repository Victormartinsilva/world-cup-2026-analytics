name: world_cup_2026
version: "1.0.0"
config-version: 2

profile: world_cup_2026

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  world_cup_2026:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
